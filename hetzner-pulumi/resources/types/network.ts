export enum NetworkRegion {
  eu = "eu-central",
  usWest = "us-west",
  usEast = "us-east",
}

export enum NetworkRole {
  lb = "load-balancer",
  cache = "varnish-cache",
  web = "webserver",
  db = "database",
}
